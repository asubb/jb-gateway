#!/bin/bash

# This script is a shortcut to SSH into the host machine from the JB Gateway container
# It automatically uses the correct host address and port for connecting to the host's SSH server

ssh host.docker.internal
